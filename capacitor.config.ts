import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'angular-capacitor-pwa',
  webDir: 'dist/angular-capacitor-pwa',
  bundledWebRuntime: false
};

export default config;
